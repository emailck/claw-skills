
- Upgraded OpenClaw from 2026.2.26 to 2026.3.2 via `npm i -g openclaw@latest`; verified `openclaw --version` and gateway status ok.
- Verified sub2api provider (`https://ai.qaq.al/v1`, api `openai-responses`) supports GPT-5.3 variants by calling `GET /models` with `SUB2API_API_KEY`: saw `gpt-5.3-codex` and `gpt-5.3-codex-spark` in the returned model list. User specifically wants plain `gpt-5.3` (non-codex), pending full model list search.
